gcp_project: "fitcv-491123"
bigquery_dataset: "fitcv"
service_account_key: "sa_key.json"

location: "us-central1"

# Option B acceptance strictness policy (SSOT for review_required downgrade).
cv_acceptance_policy:
  required_match:
    min_ratio_by_fit:
      strong: 0.80
      stretch: 0.50
    max_missing_by_fit:
      strong: 0
      stretch: 1
  force_review_when_any_required_missing_for_fits:
    - stretch

# Candidate profile path
paths:
  candidate_profile: "data/candidate_profile.private.yaml"

# Canonical ownership moved to taxonomy/runtime config surfaces:
# - seniority ladder: config/taxonomy/taxonomy.yaml
# - application statuses: canonical taxonomy/runtime owner



enrichment_version: "forced_fresh_1779195208"

